# Shared runtime dependencies across all 8 tasks' repo-root .venv.
# Task-specific extras (langgraph, chromadb, langfuse, etc.) are pinned here
# too since every task shares one venv, matching RxGround/GridScribe's
# single-venv-per-repo convention.
pydantic>=2.13
requests>=2.32
ollama>=0.6
google-genai>=2.17
groq>=1.6
pytest>=9.1
chromadb>=0.5
langgraph>=0.6
langgraph-checkpoint-sqlite>=2.0
langchain-core>=0.3
langfuse>=2.53
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp>=1.27
